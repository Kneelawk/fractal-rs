{% ifndef GLOBALS_WGSL %}
{% define GLOBALS_WGSL %}

//
// globals.wgsl.liquid - This file contains the global template-ized variables
// used everywhere else.
//

let t_c_real: f32 = {{ opts.c_real }};

let t_c_imag: f32 = {{ opts.c_imag }};

let t_iterations: u32 = {{ opts.iterations }}u;

let t_mandelbrot: bool = {{ opts.mandelbrot }};

// This constant is designed to have its value replaced.
let t_radius_squared: f32 = {{ opts.radius_squared }}.0;

// This constant is designed to have its value replaced.
let t_sample_count: u32 = 1u;

{% endifndef %}
